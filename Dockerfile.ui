# Filename: Dockerfile.ui

# Start from a slim Python base image
FROM python:3.11-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the requirements file first and install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy all our application code into the container
COPY . .

# Expose the port Streamlit runs on
EXPOSE 8501

# The command to run the Streamlit app
# We must set server.address to 0.0.0.0 to allow network connections
CMD ["streamlit", "run", "app.py", "--server.port", "8501", "--server.address", "0.0.0.0"]